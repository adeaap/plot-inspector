# Optional — canonical URL used as `metadataBase` for OG image / Twitter card
# resolution in production. Defaults to http://localhost:3000 when unset, which
# is fine for local dev but breaks absolute URLs in social previews.
# NEXT_PUBLIC_SITE_URL=https://plot-inspector.vercel.app
